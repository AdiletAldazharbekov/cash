// export async function GET() {
  // Проверка аутентификации пользователя и проверка роли
  // const session = await getSession();

  // Проверка, аутентифицирован ли пользователь
  // if (!session) {
    // return new Response(null, { status: 401 }); // Пользователь не аутентифицирован
  // }

  // Проверка, есть ли у пользователя роль 'admin'
  // if (session.user.role !== 'admin') {
    // return new Response(null, { status: 403 }); // Пользователь аутентифицирован, но не имеет права доступа
  // }

  // Получение данных для авторизованных пользователей
// }
